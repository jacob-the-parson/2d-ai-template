// Logger utility module
//export const logPlayerPosition = (player) => {
//    console.log(`Player Position: X=${player.position.x}, Y=${player.position.y}`);
//  };
  
//  export const logActiveKeys = (keys) => {
//    console.log(`Active Keys: W=${keys.w.pressed}, A=${keys.a.pressed}, S=${keys.s.pressed}, D=${keys.d.pressed}`);
//  };
  
//  export const logCollisionEvent = (collided) => {
//    if (collided) {
//      console.log('Collision detected with a boundary!');
//    }
//  };
  
//  export const logCharacterCollision = (character) => {
//    if (character) {
//      console.log(`Collision with character: ${character}`);
//    }
//  };
  
//  export const logInteraction = (interactionAsset) => {
//    console.log(`Interacting with: ${interactionAsset}`);
//  };
  